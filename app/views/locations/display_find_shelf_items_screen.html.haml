%h2
  Enter Location Name (comma separated if multiple)
=form_tag '/display_shelf_items', method: 'get' do
  =text_field_tag(:location_string, (session.key?(:location_string) ? session[:location_string] : ''), class: 'form-control input-sm')
  =submit_tag :Submit, class: 'btn btn-primary'
